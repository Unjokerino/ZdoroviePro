import { StyleSheet } from "react-native";

export default StyleSheet.create({
  card: {
    backgroundColor: "white",
    borderRadius: 10,
    padding: 13.5,
  },
  image: {
    borderRadius: 15,
    width: 60,
    height: 60,
    marginRight: 20,
  },
  mainInfo: {
    flexDirection: "row",
  },
});
